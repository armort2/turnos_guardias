[tool.ruff]
line-length = 88
target-version = "py312"
extend-exclude = [
  "migrations",
  "__pycache__",
  ".venv",
  "venv",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.isort]
combine-as-imports = true
